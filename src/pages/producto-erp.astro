---
import BaseLayout from '../layouts/BaseLayout.astro';
import Header from '../components/Header.astro';
import Footer from '../components/Footer.astro';
import CtaBlock from '../components/CtaBlock.astro';

const title = 'IAORUS ERP - El centro de operaciones de tu empresa';
const description = 'Gestiona tu empresa desde un solo lugar: compras, ventas, almacén, finanzas, RRHH, proyectos y CRM.';
const currentPage = '/producto-erp';
const p = { key: 'erp', tag: 'Producto', name: 'IAORUS ERP', desc: 'El centro de operaciones de tu empresa. Gestiona compras, ventas, almacén, finanzas, RRHH, proyectos y CRM desde un único panel.', video: 'VIDEO_ID_ERP', thumb: 'thumb-erp', fb: 'ERP', img: 'pexels-kampus-8353815.jpg' };
const features = [
  { title: 'Gestión completa', desc: 'Compras, ventas, almacén, finanzas y RRHH' },
  { title: 'CRM integrado', desc: 'Gestiona clientes y oportunidades de venta' },
  { title: 'Datos en tiempo real', desc: 'Dashboards con indicadores clave actualizados' },
  { title: 'Automatización IA', desc: 'Reduce tareas manuales hasta un 70%' }
];
const benefits = [
  { title: 'Integración total', desc: 'Todos tus departamentos conectados en una sola plataforma' },
  { title: 'Datos en tiempo real', desc: 'Toma decisiones con información actualizada al instante' },
  { title: 'Automatización inteligente', desc: 'Reduce tareas manuales hasta un 70%' },
  { title: 'Escalabilidad real', desc: 'Crece sin límites, la plataforma se adapta a ti' }
];
const steps = [
  { number: '1', title: 'Configuramos', description: 'Adaptamos IAORUS a tus procesos sin que cambies tu forma de trabajar' },
  { number: '2', title: 'Conectamos', description: 'Integramos todos tus departamentos en una sola plataforma' },
  { number: '3', title: 'Transformamos', description: 'Empieza a operar con más eficiencia desde el día uno' }
];
const demoHref = `/demo?producto=${p.key}`;
---

<BaseLayout title={title} description={description} currentPage={currentPage}>
  <Header slot="header" currentPage={currentPage} />

  <section class="video-hero">
    <div class="video-hero-bg"></div>
    <div class="container">
      <div class="video-hero-content">
        <div class="video-hero-text">
          <span class="hero-tag">Producto</span>
          <h1>{p.name}</h1>
          <p>{p.desc}</p>
          <div class="hero-actions" style="justify-content:flex-start;">
            <a href={demoHref} class="btn btn-demo">Solicitar demo gratuita →</a>
          </div>
        </div>
        <div class="video-thumb-wrapper" onclick={`openVideo('https://www.youtube.com/embed/${p.video}', '${p.name}')`}>
          <img src={`/assets/images/thumbnails/${p.thumb}.jpg`} alt={p.name} onerror={`imgFallback(this,'${p.fb}')`}>
          <div class="video-thumb-overlay">
            <div class="play-btn"><svg width="32" height="32" viewBox="0 0 24 24" fill="currentColor"><polygon points="5 3 19 12 5 21 5 3"/></svg></div>
          </div>
        </div>
      </div>
    </div>
  </section>

  <section class="bg-gray">
    <div class="container">
      <div class="split-grid">
        <div>
          <img src={`/assets/images/${p.img}`} alt={`Panel ${p.name}`} class="img-section">
        </div>
        <div>
          <span class="section-tag" style="background:rgba(244,121,32,0.15);color:var(--orange);">Características</span>
          <h2>Características principales</h2>
          <div class="landing-benefits" style="grid-template-columns:1fr 1fr;">
            {features.map(f => (
              <div class="landing-benefit">
                <div class="landing-benefit-icon"><svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="#F47920" stroke-width="2"><polyline points="20 6 9 17 4 12"/></svg></div>
                <h4>{f.title}</h4>
                <p>{f.desc}</p>
              </div>
            ))}
          </div>
        </div>
      </div>
    </div>
  </section>

  <section>
    <div class="container">
      <div class="section-title">
        <span class="section-tag" style="background:rgba(244,121,32,0.15);color:var(--orange);">Beneficios</span>
        <h2>¿Por qué {p.name}?</h2>
        <p>Todo lo que necesitas para gestionar tu empresa, en un solo lugar.</p>
      </div>
      <div class="landing-benefits">
        {benefits.map(b => (
          <div class="landing-benefit">
            <div class="landing-benefit-icon"><svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="#F47920" stroke-width="2"><polyline points="20 6 9 17 4 12"/></svg></div>
            <h4>{b.title}</h4>
            <p>{b.desc}</p>
          </div>
        ))}
      </div>
    </div>
  </section>

  <section>
    <div class="container">
      <div class="section-title">
        <span class="section-tag" style="background:rgba(244,121,32,0.15);color:var(--orange);">Vídeo</span>
        <h2>Vídeo demostrativo</h2>
        <p>Descubre cómo funciona {p.name} en la práctica.</p>
      </div>
      <div class="video-placeholder">
        <div class="vp-icon"><svg width="48" height="48" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5"><polygon points="5 3 19 12 5 21 5 3"/></svg></div>
        <p class="vp-text">Vídeo demostrativo disponible próximamente</p>
      </div>
    </div>
  </section>

  <section class="bg-gray">
    <div class="container">
      <div class="section-title">
        <span class="section-tag" style="background:rgba(244,121,32,0.15);color:var(--orange);">Cómo funciona</span>
        <h2>Tu nuevo ERP en 3 pasos</h2>
        <p>Te acompañamos en cada etapa para una transición sin fricción.</p>
      </div>
      <div class="landing-steps">
        {steps.map(s => (
          <div class="landing-step">
            <div class="landing-step-num">{s.number}</div>
            <h4>{s.title}</h4>
            <p>{s.description}</p>
          </div>
        ))}
      </div>
    </div>
  </section>

  <CtaBlock
    title="¿Listo para transformar tu negocio?"
    subtitle={`Solicita una demo personalizada y descubre cómo ${p.name} puede impulsar tu empresa.`}
    ctaPrimary={{ text: 'Solicitar demo gratuita →', href: demoHref }}
    ctaSecondary={{ text: 'Hablar con un especialista', href: '/contacto' }}
    variant="landing-cta-section"
  />

  <CtaBlock
    variant="floating"
    title="¿Listo para transformar tu negocio?"
    ctaPrimary={{ text: 'Solicitar demo →', href: demoHref }}
    ctaSecondary={{ text: 'Contactar', href: '/contacto' }}
  />

  <Footer slot="footer" />
</BaseLayout>
